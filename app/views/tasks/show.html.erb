<h2><%= @task.title %></h2>
<p><%= @task.details %></p>

<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>

<%= link_to "Edit", task_edit_path(@task) %>

<%= link_to "Return to task list", all_tasks_path(@task) %>
